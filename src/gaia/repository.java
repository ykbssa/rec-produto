package gaia;

/// import br.com.produto.model.nome;
/// import org.springframework.data.jpa.repository.Prepository;

import java.util.List;

///  interface repository extends Prepository<produto> {
   /// public list<produto> findbynome(String nome);
/// }